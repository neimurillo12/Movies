{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File (Integrated Terminal)",
            "type": "python",
            "request": "launch",
            "module": "uvicorn",
            "args": ["main:app"],
            "cwd": "${workspaceFolder}"
        }
    ]
}